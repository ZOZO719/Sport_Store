spring.application.name=store
server.port=8090

# ── PostgreSQL (production) ──────────────────────────────────────────────────
spring.datasource.url=jdbc:postgresql://localhost:5432/Store
spring.datasource.username=postgres
spring.datasource.password=zaidkassem
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# ── H2 Console (dev) — BUG FIX: console was never enabled ───────────────────
# To use H2 instead of Postgres in dev, comment the postgres block above
# and uncomment the H2 block below, then switch ddl-auto to create-drop.
#spring.datasource.url=jdbc:h2:mem:storedb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
#spring.jpa.hibernate.ddl-auto=create-drop
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console